A computer is an advanced electronic data-processing device that takes raw data as input from the user, 
processes it under the control of a set of instructions (called a program), 
produces a structured result (output), and saves it for future use (storage).
